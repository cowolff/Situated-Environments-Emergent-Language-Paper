from setuptools import setup, find_packages

setup(
    name='LanguageOpenEnd',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        # Any dependencies your package needs,
        # e.g., 'requests >= 2.23.0',
    ],
    author='Anonymous',
    author_email='mail@mail.de',
    description='A package for my paper on open-ended language learning',
)